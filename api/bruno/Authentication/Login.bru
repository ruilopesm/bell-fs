meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{host}}/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "pdf",
    "password": "pedro123152364537"
  }
}

script:post-response {
  if (res.getStatus() === 200) {
    let data = res.getBody();
    
    bru.setEnvVar("access_token", data.access_token);
    bru.setEnvVar("refresh_token", data.refresh_token);
  }
}
