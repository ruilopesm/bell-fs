meta {
  name: Create file
  type: http
  seq: 2
}

post {
  url: {{host}}/files
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  confidentiality: {{confidentiality_name}} @contentType(string)
  integrity: {{integrity_name}} @contentType(string)
  persistent: @file(/home/rui/dotfiles/README.md)
}
